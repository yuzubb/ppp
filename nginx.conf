worker_processes auto;
error_log /dev/stdout info;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    access_log    /dev/stdout;

    # IPv6を完全に無視し、Google DNSでIPv4のみ解決する
    resolver 8.8.8.8 ipv6=off;

    server {
        listen $PORT;

        # 共通のプロキシ設定
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header User-Agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36";
        proxy_set_header Referer "https://mangafire.to/";

        # 1. 漫画画像系のパス (img.mangafire.to へ)
        # ログから推測される画像・メディアパスを網羅
        location ~* ^/(manga|images|covers|avatar|media|contents)/ {
            # upstreamを使わず直接記述してIPv6回避を確実にする
            proxy_pass https://img.mangafire.to;
            proxy_set_header Host img.mangafire.to;
            
            add_header Access-Control-Allow-Origin *;
            expires 7d;
        }

        # 2. Ajax・API・システム系のパス (mangafire.to へ)
        # ログに出ていた /ajax/ をここで確実に捕まえる
        location ~* ^/(ajax|api|assets|js|css|manifest\.json|sw\.js)/ {
            proxy_pass https://mangafire.to;
            proxy_set_header Host mangafire.to;
            
            proxy_set_header Accept-Encoding ""; 
            sub_filter 'https://mangafire.to' '$scheme://$host';
            sub_filter 'mangafire.to' '$host';
            sub_filter_once off;
            sub_filter_types *;
        }

        # 3. メインページとその他の全パス
        location / {
            proxy_pass https://mangafire.to;
            proxy_set_header Host mangafire.to;

            # レスポンス内のリンクを自分のドメインに書き換え
            proxy_set_header Accept-Encoding "";
            sub_filter 'https://mangafire.to' '$scheme://$host';
            sub_filter 'mangafire.to' '$host';
            sub_filter_once off;
            sub_filter_types text/html text/css text/javascript application/javascript application/json;

            proxy_redirect https://mangafire.to/ /;
            proxy_cookie_domain mangafire.to $host;
        }
    }
}
